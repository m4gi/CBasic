#include <stdio.h>
#include <conio.h>
#include <stdlib.h>

int main(int argc, char const *argv[])
{
    FILE *fi;
    fi = fopen("vidu.inp","rb");
    int a,b,n;
    a=0; b=0;
    while(fread(&i,sizeof(int),1,fi)==1)
        scanf("%d", &n);
    fscanf(fi,"%d", %n);
    for(i=1;i<=n;i++)
    {
        scanf()
    }
    return 0;
}